NEXT_PUBLIC_API_MOCKING=enabled
# 애플 로그인. 프로덕션은 배포 워크플로 Build 스텝이 켠다(deploy-prod.yml).
# 여기 없으면 `next dev`(= dev:e2e)에서만 버튼이 사라져 Cypress 가 프로덕션과
# 다른 화면을 보게 된다 — 랜딩 레이아웃 회귀를 잡으려면 같은 상태여야 한다.
NEXT_PUBLIC_APPLE_LOGIN_ENABLED=true
